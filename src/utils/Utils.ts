const toUpper = (str: string) => str.toUpperCase();

export {
    toUpper,
    // Add more utility functions here
    // Example: const toLower = (str: string) => str.toLowerCase();
    // export { toLower };
}